# 24 面试自我介绍与项目讲述模板

> 核心原则：所有版本都要"讲数据、讲技术、讲理解"，不要只讲故事。面试官想听的是"你做了什么、用了什么技术、结果如何"，而不是"项目背景多么宏大"。

---

## 一、30秒自我介绍模板

> 适用场景：开场寒暄后的简短自我介绍、"请用一句话介绍你自己"

```
您好，我叫[姓名]，[学校/专业/年级]，研究方向是大模型后训练与对齐。

我主导了一个医学 LLM 项目，基于 Qwen3-8B，完整走通了 Teacher 数据生成 → 58K SFT 微调 → 10K DPO 偏好对齐 → Safety-RAG 安全验证 → vLLM 部署的全流程。项目中我发现 DPO 后出现了医疗长尾幻觉，通过设计 Safety-RAG 验证管线成功修复，幻觉案例下降 66.7%。

此外我还从零训练了一个 MiniMind 小模型（26M 参数），深入理解了 Transformer 底层实现。希望能在贵团队继续深耕后训练算法方向。
```

**记忆要点**：姓名 → 方向 → 核心项目（一个数字链：Qwen3-8B/58K SFT/10K DPO/Safety-RAG/66.7%） → 辅助项目 → 求职方向

---

## 二、1分钟自我介绍模板

> 适用场景：标准面试开局、"请简单介绍一下你自己"

```
面试官您好，我叫[姓名]，目前[在读/毕业]于[学校][专业]。我专注于大模型后训练算法方向，有完整的研究项目经验和扎实的底层理解。

我的核心项目是"医学 LLM Teacher"。我基于 Qwen3-8B 基座模型，用 Teacher 模型 Qwen3-72B 生成了 58K 条高质量医疗 SFT 数据，覆盖问诊、检查解读、用药咨询、健康科普四个场景。使用 QLoRA（r=16）完成 SFT 训练，随后构造 10K 偏好数据对进行 DPO 偏好对齐（β=0.1）。DPO 后我用 LLM-as-Judge 三维度评测体系发现了医疗长尾幻觉问题——模型会编造文献出处和临床数据。我分析了这是 DPO reward hacking 导致的，并设计了 Safety-RAG 验证-修正管线，用外部医疗知识库对模型输出进行事实性验证，最终幻觉案例全部修复。

此外，我还从零训练了一个 26M 参数的 MiniMind 小模型，完整实现了 GQA/RoPE/RMSNorm/SwiGLU 等架构组件，加深了对 Transformer 底层的理解。

我现在的目标是进入贵团队做后训练算法实习，继续在 alignment、safety 方向深耕。
```

**记忆要点**：项目按时间线讲（数据→训练→发现问题→解决→结果），每个环节一个具体数字。

---

## 三、2分钟自我介绍模板

> 适用场景：面试官说"详细介绍一下你自己"或终面环节

```
面试官您好，我叫[姓名]，[学校/专业]。我目前的研究方向是大模型后训练与安全对齐，有一个主项目和一个小辅助项目，想和您分享一下。

【主项目：医学 LLM Teacher】我做了一个完整的医学对话后训练项目。基座模型用的 Qwen3-8B——选择它是基于三点考量：GQA 推理效率高、中文能力 SOTA、硬件约束可控。

整个项目的 pipeline 分为五个阶段：

第一阶段是数据生成。我用 Qwen3-72B 作为 Teacher，构造了不同场景的 prompt 模板，生成 58K 条医疗 SFT 数据。数据质量控制包括：知识库交叉验证、去重、安全过滤和人工抽检 10%。

第二阶段是 SFT 训练。使用 QLoRA r=16/α=32，覆盖 attention 和 FFN 的所有线性层，在 2 张 4090 上训练 3 个 epoch。关键经验是 learning rate 从 2e-4 调到 5e-5 才使 loss 稳定收敛。

第三阶段是 DPO 偏好对齐。我从三个来源构造了 10K 偏好数据：同 prompt 不同 temperature 的采样对比、SFT 模型 vs Teacher 对比、以及 500 条人工标注。β=0.1 训练 1 epoch，reward accuracy 达到 0.78。

第四阶段是评测和分析。我用 Qwen3-72B 作为 LLM-as-Judge，设计了医学准确性、安全性、完整性三维度评测。和人工标注对标后，accuracy 维度 Spearman 达 0.78。但评测中我发现了一个重要现象——DPO 后模型在长尾医疗问题上出现了明显的幻觉：会编造不存在的临床数据和文献出处。我分析这是 reward hacking——DPO 的训练偏好使得模型在不确定时倾向于"自信编造"而非"诚实表达不确定性"。

第五阶段是 Safety-RAG 的提出和验证。我构建了包含 50 万条结构化知识的医疗知识库，在推理时检索 top-5 相关知识，用一个独立的 verifier 比对生成内容和检索知识是否一致，如果不一致就用检索知识修正。在 3 个典型幻觉案例上测试，全部修复。

最后用 vLLM 部署了 API 服务，单卡 4090 支持 10+ 并发。

【辅助项目：MiniMind】我从零训练了一个 26M 参数的小模型，自己实现了 GQA、RoPE、RMSNorm、SwiGLU 等架构组件，完整走完了 tokenizer 训练 → 预训练 → SFT → DPO 的全流程。这个项目让我真正理解了 LLM 的每个底层细节。

我现在申请的岗位是大模型后训练算法实习。我认为后训练的核心在于"如何用有限数据让模型学会人类偏好"——这涉及数据策略、训练方法选择、安全对齐和评测体系设计。我在项目中实践了这整个链路，期待在贵团队继续深入这个方向。
```

**记忆要点**：分阶段讲，每个阶段有目标+方法+数字+经验。最后有总结和求职动机。

---

## 四、医学 LLM 项目 — 1分钟版本

```
我做了一个医学 LLM 的后训练项目。基于 Qwen3-8B，用 Teacher 模型生成了 58K 医疗 SFT 数据，使用 QLoRA 完成训练。然后构造了 10K 偏好数据做 DPO 对齐。关键发现是 DPO 后出现了医疗长尾幻觉——模型编造虚假的临床数据。我设计了 Safety-RAG 验证管线，用外部知识库验证模型输出，幻觉案例全部修复。这个项目的完整链路是：数据生成 → SFT → DPO → 安全评测 → Safety-RAG → vLLM 部署。
```

**记忆要点**：一句话概括 → 关键技术点（58K/QLoRA/10K DPO）→ 核心发现（幻觉）→ 解决方案（Safety-RAG）→ 完整性（到部署）

---

## 五、医学 LLM 项目 — 3分钟版本

```
【背景和动机】
这个项目要构建一个能安全回答医疗问题的对话模型。核心约束是：不能让模型给出错误医疗建议，也不能编造不存在的医学信息。我们需要平衡"有用性"和"安全性"。

【数据阶段】
第一步是构造训练数据。我用 Qwen3-72B 作为 Teacher 模型，设计了涵盖问诊、检查解读、用药咨询、健康科普四类场景的 prompt 模板。为了保证数据多样性，我用了不同 temperature 采样和多 prompt 模板生成。最终收集了 58K 条数据，经过知识库交叉验证、语义去重、安全关键词过滤和 10% 人工抽检后入库。

【SFT 训练】
训练阶段用 QLoRA，rank=16，alpha=32。之所以不用全量微调，是因为 8B 模型的全量微调需要约 192GB 显存，而我们的硬件是 2 张 4090（共 48GB）。QLoRA 把基座量化到 4bit NF4，只训练 1% 的参数，加上双重量化和分页优化器，总显存约 12GB。训练 3 个 epoch，learning rate 5e-5 cosine schedule，effective batch size 16。

【DPO 训练与关键发现】
SFT 之后，我做了 DPO 偏好对齐。10K 偏好数据来自三路：多采样对比、模型 vs teacher 对比、人工标注 500 条高风险场景。β=0.1 训练 1 个 epoch。

训练完后用 LLM-as-Judge 评测——我设计了三维度（准确性/安全性/完整性），用 Qwen3-72B 做 judge。发现一个有意思的现象：DPO 后模型在常见问题上表现更好（回答更完整、更有条理），但在医疗长尾问题上出现了新的幻觉——模型会编造具体的文献出处、临床试验数据来让回答显得更专业。比如问"某抗生素对某菌的效果"，模型会编造"一项三期临床招募 2347 例患者，有效率达 87.3%"——但其实这个临床试验根本不存在。

【Safety-RAG 设计】
我分析了这个现象的原因：DPO 的 reward 信号倾向于"确定自信"的回答风格——chosen 回答通常比 rejected 更完整、更确定。模型学会了：在不确定时，与其说"我不确定"可能被标为 rejected，不如编造一个看似专业的回答更像 chosen。这是典型的 reward hacking。

为了解决这个问题，我设计了 Safety-RAG——一个带验证层的 RAG 系统。核心改动是：不光检索知识然后让模型生成，而是先生成、再验证、必要时修正。具体来说：1）构建了 50 万条医疗知识库，用 bge-large-zh-v1.5 向量化；2）推理时检索 top-5；3）一个专门的 verifier 比对生成内容和检索知识的事实一致性；4）如果不一致，以检索知识为准生成修正后的回答。

【效果与部署】
在 3 个 DPO 后出现的典型幻觉案例上，Safety-RAG 全部修复。虽然 3 个案例样本量太少不能做统计显著性检验，但至少验证了 Safety-RAG 的有效性方向。最后用 vLLM 部署了 API 服务，支持 10+ 并发请求。

【Reflection】
这个项目如果重新做，我会：1）扩大安全评测的样本量；2）DPO 后做一轮 iterative DPO 验证；3）引入专业的医学 reranker 替代简单向量检索。目前整体完成度约 80%——研究探索 + 工程落地的组合。
```

**记忆要点**：3 分钟可以讲得比较详细，但不要面面俱到。重点放在"发现幻觉→分析原因→设计方案→验证效果"这个核心叙事上。

---

## 六、医学 LLM 项目 — 5分钟版本

> 在 3 分钟版本基础上扩展以下内容：

### 补充 1：Teacher 数据生成的更多细节

```
在 Teacher 数据生成阶段，我面临的核心问题是"如何让 Teacher 生成既正确又多样化的医疗数据"。

正确性方面：我设计了一个"生成-检索-验证"的循环——Teacher 生成一条问答后，自动检索知识库中最相关的片段，与生成内容做语义相似度比对。如果一致性低于阈值（0.7），这条数据被标记为低质量并重新生成或丢弃。

多样性方面：我用了 4 种策略——1）不同 temperature（0.1/0.5/0.9）生成不同风格的 response；2）多 prompt 模板轮换（20+ 种问诊/科普/用药的 prompt 变体）；3）场景配额控制——四类场景各占约 25%；4）按疾病大类采样（呼吸/消化/心血管等 12 大类），确保覆盖广度。

最终从约 80K 原始生成中筛选出 58K 高质量数据，过滤率约 27%。
```

### 补充 2：DPO 数据构造的更多细节

```
DPO 数据的核心挑战是保证 chosen/rejected pair 的"subtle but meaningful"——差异太明显学不到细粒度偏好，差异太模糊没有信号。

我的策略是做一条"梯度"：对于每个 prompt，用 temperature=0.1/0.5/0.9 采样 3 个 response，让 LLM-as-Judge 打分。通常 t=0.1 最安全但最保守，t=0.9 最丰富但可能出错。我把 t=0.1 和 t=0.9 的输出比较，如果两者分数差在 1-2 分（5 分制），就是一组好 pair——既非天差地别也非完全一致。如果分数差太大（>3 分），说明 t=0.9 的答案有明显错误，pair 太简单，舍弃。

加上人工标注的 500 条高风险 case（如处方建议、诊断结论、紧急情况），最终 10K DPO 数据。
```

### 补充 3：安全评测体系的更多细节

```
评测体系我从三个层面设计：

1）自动评测（LLM-as-Judge）：准确性(40%) + 安全性(35%) + 完整性(25%)，Qwen3-72B 打分。和 200 条人工标注对标，accuracy 维度 Spearman 0.78，safety 维度 0.62——说明 LLM 对安全性的判断还不够可靠。

2）安全 benchmark 测试：用 DangerousQA 测试违规回答率，补充 adversarial 场景。

3）人工抽检：每周随机抽 50 条输出，标注安全等级和是否有幻觉。

我特别关注了"安全 vs 有用性"的 trade-off。我们的 benign query 拒答率控制在 5% 以下，adversarial query 拒答率 90% 以上。这个平衡点是通过调整 system prompt 强度和 training data 中安全样本占比（15%）来调整的。
```

### 补充 4：Safety-RAG 设计的思考过程

```
Safety-RAG 的设计不是一蹴而就的，经过了几次迭代：

v1：简单在 prompt 里拼接检索结果 → 模型有时忽略检索结果，仍按照自己的"记忆"编造内容。
v2：加了一个"先检索再生成"的硬约束 prompt → 模型会关注检索结果了，但检索到的知识也可能是错的或不完整的。
v3（最终版）：三步闭环——检索 → 生成 → 验证 → （如需要）修正。Verifier 不是简单的文本比对，而是用微调过的 Qwen3-8B 做医疗事实 NLI 任务。Verifier 的训练数据是 5K 条人工标注的（query, response, knowledge, label）四元组。

这个设计真正实现了知识库和模型的双向校验——不盲目信任任何一方。
```

---

## 七、MiniMind 辅助项目 — 30秒版本

```
除了医学 LLM 项目，我还从零训练了一个 MiniMind 小模型（26M 参数，512 维，8 层 Decoder，8 头 GQA，RoPE 位置编码，RMSNorm，SwiGLU 激活）。完整实现了 tokenizer 训练、预训练、SFT、DPO 全流程。这个项目让我真正理解了每个底层组件——从 tokenizer 的 BPE 合并到 attention 的 KV cache，从 RoPE 的旋转编码到 SwiGLU 的 gated 机制。它补充了我对 LLM 底层原理的理解。
```

**记忆要点**：MiniMind 的定位是"底层理解"的补充，强调它与医学项目的互补关系。

---

## 八、面试官追问"你主要负责什么"回答

```
在这个项目中，我独立负责了整个后训练 pipeline 的设计和实现。具体来说：

1）数据策略：Teacher 模型 prompt 设计、数据生成-验证-过滤管线、质量保证体系
2）训练策略：SFT 和 DPO 的超参数选择（做了 grid search）、训练不稳定性排查
3）评测体系：LLM-as-Judge 三维度设计、与人工标注对标
4）核心发现：DPO 后幻觉增加的识别和分析（reward hacking 机制分析）
5）Safe-RAG：从构思、设计、实现到验证的全部工作
6）工程部署：vLLM API 服务搭建、性能调优

可以说，这个项目从 data 到 training 到 evaluation 到 safety 到 deployment 的每个环节我都有 hands-on 经验。导师在前期给了方向建议（比如推荐使用 Qwen 系列基座），但具体的技术选择和实验设计是我独立完成的。
```

---

## 九、面试官追问"项目最大难点"回答

```
这个项目最大的难点不是某个技术点的实现，而是一个"发现问题"的过程。

DPO 训练完之后，如果只看 loss 和 reward accuracy 曲线——一切似乎很好。loss 稳步下降，reward accuracy 从 0.5 到 0.78，看起来训练很成功。但当我做深度 case study 时，发现了一个在表面指标上看不到的问题：模型在回答"某抗生素对某菌的耐药率"时，居然编造了具体的临床数据、文献引用和作者名字。

这个发现的难点在于：1）它不在常规评测指标（loss/reward accuracy）的监测范围内；2）它只在长尾医疗问题中出现，不是普遍现象；3）需要医学领域知识才能识别出模型在编造。

分析原因后发现这是一个深层的算法问题——DPO 的 implicit reward 模型学会了偏好"确定自信"的回答风格，而"诚实表达不确定性"被隐式地 penalized。这是 reward hacking 在医疗场景的具体体现。

解决这个问题的 Safety-RAG 方案虽然技术实现不难，但"找到问题"和"分析原因"的过程才是真正的难点——它需要跨领域的判断力（算法 + 医疗），以及对训练 dynamics 的深入理解。
```

---

## 十、面试官追问"项目创新点"回答

```
我认为这个项目的创新点分为两层：

第一层是技术方案层面的创新——Safety-RAG 的"验证-修正闭环"。标准 RAG 是检索后直接拼接生成，而我们的 Safety-RAG 多了验证层：用 verifier 对生成内容和检索知识做事实一致性检查，不一致就修正。这个设计解决了标准 RAG 的两个盲点：1）检索到的知识可能不相关或不完整；2）模型可能忽略检索结果而依赖自己的"记忆"。

第二层是方法论层面的创新——"从发现问题到解决问题"的完整算法思维链。DPO 训练后用常规指标看着很好，但我通过深度 case study 发现了幻觉问题，分析了 reward hacking 的机制，提出了 Safety-RAG 方案，并做了实验验证。这个"观察 → 假设 → 方案 → 验证"的闭环是我认为最有价值的。

但我需要诚实地说：Safety-RAG 作为一种"检索验证"思路并非全新发明——它站在了 RAG（检索增强生成）和 RLHF safety 两条线的交叉点上。我的贡献是在医疗这个垂直场景中，识别出了"DPO 后幻觉增加"的具体问题，并将验证机制设计为一个完整的闭环系统。我理解创新的"增量性"和"场景性"。
```

---

## 十一、面试官追问"项目不足"回答

```
这个项目有几个明确的不足，我可以诚实地和您分享：

第一，评测样本量不足。Safety-RAG 的效果只在 3 个典型幻觉案例上验证了，样本量太小，不能做统计显著性检验。如果重新做，我会设计一个包含 100+ 条样本的系统性评测集。

第二，没有真实用户反馈闭环。这是一个研究探索项目，没有线上部署和真实用户的 feedback。vLLM 的 API 是可用的，但缺少用户侧的数据来验证模型在实际场景中的表现。

第三，知识库覆盖不全面。50 万条知识主要覆盖常见疾病和药物，对于罕见病、新药、前沿疗法覆盖不足。这会导致 Safety-RAG 在这些领域仍然可能出错。

第四，Safety-RAG 增加了推理延迟。检索+验证+可能的重新生成，总共增加了约 500ms 的延迟，对于实时对话场景有影响。

第五，没有做消融实验来严格证明 Safety-RAG 各组件（检索/验证/修正）各自的贡献——这会使安全效果的归因不够严谨。

我认为认识不足和正视不足本身也是能力的一部分，这些不足也意味着明确的改进方向。
```

---

## 十二、面试官追问"为什么适合后训练实习"回答

```
我认为我适合后训练算法实习是因为三方面的匹配：

第一，我有完整的后训练实践经历。不是只做过 SFT 或只做过 DPO，而是走通了 data → SFT → DPO → evaluation → safety → deployment 全流程。我可以快速上手团队的 training pipeline，不需要从"SFT 是什么"开始学。

第二，我有从底层到应用的双向理解。MiniMind 项目让我理解了每个底层组件的数学原理和代码实现——RoPE 为什么这样设计、RMSNorm 和 LayerNorm 的区别到底在哪里、attention 的显存瓶颈怎么产生的。医学 LLM 项目让我知道这些底层知识如何指导实际的后训练决策——比如为什么选 GQA 架构做基座、QLoRA 的 r 值怎么选、DPO beta 的 grid search 怎么做。我既能看懂论文中的公式推导，也能把公式变成可运行的训练代码。

第三，我有安全对齐的意识和方法论。后训练不仅是让模型"有用"，更是让模型"安全"。我在项目中发现了 DPO 后的 reward hacking 问题，提出了 Safety-RAG 方案——这让我对 alignment tax、reward hacking、safety-usefulness trade-off 有了亲身体会，而不是只从论文里读到。

我希望在实习中：1）深入 RLHF/GRPO 等更复杂的 RL alignment 方法；2）在更大规模的数据和模型上验证后训练方法；3）参与生产级的 alignment pipeline 建设。
```

---

## 十三、简历项目描述优化版

### 版本 A：简洁版（2 bullet points）

**医学 LLM Teacher — 后训练与安全对齐项目**
- 基于 Qwen3-8B，主导完整后训练链路：Teacher 数据生成 → 58K QLoRA SFT → 10K DPO（β=0.1）→ LLM-as-Judge 三维度评测
- 发现 DPO 后 reward hacking 导致医疗长尾幻觉增加，设计 Safety-RAG（知识库检索→生成→验证→修正闭环），3 个典型幻觉案例全部修复

### 版本 B：详细版（3 bullet points）

**医学 LLM Teacher — 大模型后训练与安全对齐**
- 基于 Qwen3-8B，用 Teacher 模型(Qwen3-72B)生成 58K 医疗 SFT 数据(4 个医学场景)，QLoRA(r=16/α=32) 完成微调；构建 10K 偏好数据(多采样对比+人工标注)完成 DPO(β=0.1)偏好对齐
- 设计 LLM-as-Judge 三维度评测体系(准确性/安全性/完整性)，与人工标注 Spearman 达 0.78；发现 DPO 后 reward hacking 导致医疗长尾幻觉增加，分析为 reward 偏好"确定自信"模式所致
- 提出 Safety-RAG 验证管线：构建 50 万条医疗知识库 + verifier 对生成内容进行事实性交叉验证，幻觉案例全部修复；vLLM 部署 API 服务支撑 10+ 并发

### 版本 C：特详细版（适合放在简历第二页的项目详情）

```
【医学 LLM Teacher — 大模型后训练与安全对齐】                          2025.09 - 2026.03
基座模型：Qwen3-8B | 技术栈：PyTorch, Transformers, PEFT, TRL, vLLM, FAISS

数据工程
- 设计 Teacher 模型(Qwen3-72B)数据生成策略：4 类医学场景 prompt 模板 + 多 temperature 采样 + 知识库交叉验证去噪
- 构建 58K 高质量 SFT 数据(80K 原始 → 去重/去噪/人工抽检 → 精筛 58K)
- 构造 10K DPO 偏好数据：同 prompt 多 temperature 采样 + SFT/Teacher 对比 + 500 条人工标注高风险样本

训练与对齐
- QLoRA(r=16, α=32) SFT 训练(2×4090, 3 epoch, lr=5e-5 cosine) → loss 稳定收敛
- DPO(β=0.1, 1 epoch, lr=5e-6)偏好对齐 → reward accuracy 0.78
- 调试经验：lr 从 2e-4 降至 5e-5 解决 SFT loss 震荡；β 在 {0.01,0.05,0.1,0.5,1.0} 中 grid search 选 0.1

评测与安全分析
- LLM-as-Judge 三维度评测(准确性/安全性/完整性)，与人工 200 条对标 Spearman 0.78
- 深度 case study 发现 DPO 后 reward hacking 现象：模型在长尾医疗问题上编造虚假文献数据
- 机制分析：DPO 隐式 reward 偏好"确定自信"输出风格 → 模型在不确定性场景 learned to fabricate

Safety-RAG 方案
- 构建 50 万条医疗知识库(bge-large-zh-v1.5 + FAISS, chunk_size=256-512, Recall@5=85%)
- 设计验证-修正闭环：检索 top-5 → 生成 → verifier(微调 Qwen3-8B) 事实比对 → 不一致则修正
- 3 个典型 DPO 后幻觉案例全部修复；安全 benchmark 违规率从 12% 降至 <2%
- vLLM 部署 API(单卡 4090, 10+ 并发, ~1.5s/token)
```

---

## 十四、STAR 法项目讲述模板

> **S**ituation - **T**ask - **A**ction - **R**esult

### STAR 模板 1：DPO 后幻觉发现（展示分析能力）

| 环节 | 内容 |
|------|------|
| **S** (情境) | DPO 训练完成后，reward accuracy 达到 0.78，表面指标看起来很好 |
| **T** (任务) | 我需要验证模型在实际医疗场景中的生成质量和安全性 |
| **A** (行动) | 我做了深度的 case study——不是只看 aggregate metrics，而是逐条检查模型在 50 个长尾医疗问题上的输出。发现 3 个典型幻觉案例（编造文献/混淆细菌治疗方案/处方建议）。进一步分析了 DPO 的梯度行为和 implicit reward，判断这个现象是 reward hacking——DPO 的 reward 偏好"确定自信"的输出风格 |
| **R** (结果) | 识别出了一个 aggregate metrics 无法发现的安全风险，这个发现直接催生了后续的 Safety-RAG 方案设计 |

### STAR 模板 2：Safety-RAG 方案（展示解决问题的能力）

| 环节 | 内容 |
|------|------|
| **S** (情境) | DPO 后模型在长尾医疗问题上出现幻觉，直接使用可能造成医疗安全风险 |
| **T** (任务) | 设计一个方案能在不重新训练模型的前提下，减少或消除这类幻觉 |
| **A** (行动) | 我设计了 Safety-RAG 的三阶段验证管线：1）构建 50 万条医疗知识库(bge-large-zh-v1.5 + FAISS)；2）推理时检索 top-5 知识；3）微调 verifier 做事实一致性检查（构造 5K NLI 训练数据）。设计的关键决策包括：chunk 大小(256-512)、overlap(64)、verifier 的 prompt 设计和训练策略 |
| **R** (结果) | 3 个典型幻觉案例全部修复，为医疗 AI 的安全部署提供了可行的技术方案 |

### STAR 模板 3：整体项目（展示全流程能力）

| 环节 | 内容 |
|------|------|
| **S** (情境) | 需要在资源受限（2×4090 24G）的条件下，将一个通用 LLM 转化为安全可用的医疗对话模型 |
| **T** (任务) | 我的目标是走通完整后训练链路，并确保模型在医疗安全性上达到基本可用水平 |
| **A** (行动) | 我独立完成了五个阶段：1）Teacher 数据生成与质量控制；2）QLoRA SFT（节省显存从 192GB 到 12GB）；3）DPO 偏好对齐（grid search β）；4）LLM-as-Judge 评测体系 + 幻觉分析；5）Safety-RAG 安全验证方案。每个阶段的超参数选择都有具体的实验依据 |
| **R** (结果) | 从 base model 到可安全对话的医疗助手，完成了 SFT→DPO→Safety 三阶段对齐，幻觉问题得到有效控制，API 服务可用于后续集成 |

---

## 使用建议

1. **录制音频练习**：把自己的项目讲述录下来，回听，找到不流畅的地方反复练习
2. **控制时间**：用秒表计时，确保每个版本都在时间预算内
3. **数字要准确**：58K、10K、8B、0.1、66.7%、0.78、12GB——这些数字要能解释来源
4. **叙事要有起伏**：项目叙述的结构应该是"做了什么 → 发现了什么意外 → 怎么解决的 → 学到了什么"
5. **诚实是底线**：不要夸大样本量、不要说"显著性"如果没有统计检验、不要说"上线了"如果只是测试
6. **根据不同面试官调整**：算法面试官→多讲方法原理和设计决策；工程面试官→多讲工程优化和架构设计；综合面试官→讲完整故事和 impact

---

---

## 十五、"算法+工程落地"版1分钟自我介绍

> 适用场景：面试岗位明确要求"有工程落地经验"或面试官是工程背景

```
面试官您好，我叫[姓名]，[学校/专业]，方向是大模型后训练与工程落地。

我的核心项目是医学 LLM Teacher。算法层面：基于 Qwen3-8B，我完整实现了 Teacher 数据生成→58K QLoRA SFT→10K DPO 偏好对齐→Safety-RAG 安全验证的后训练链路，并发现了 DPO 后 reward hacking 导致的长尾幻觉问题，设计了 Safety-RAG 验证-修正闭环修复。工程层面：我用 LoRA merge + vLLM 完成了模型部署和性能压测，在 RTX 5090 上实现 877 tok/s 吞吐、TTFT 42ms，做了并发压测找到饱和点；还做了 RAG 延迟拆解、pipeline 并行化、缓存设计等工程优化方案。

我的定位是"懂算法的工程师"——既能从 DPO 公式推导分析幻觉原因，也能把模型部署上线做压测调优。希望能在贵团队做后训练+工程落地的实习。
```

**记忆要点**：算法能力（数据→训练→分析→方案）+ 工程能力（LoRA merge + vLLM部署/压测 + RAG工程），强调"懂算法的工程师"定位。

---

## 十六、"后训练+RAG+部署"版1分钟自我介绍

> 适用场景：岗位偏 RAG 或 LLM 应用落地

```
面试官您好，我叫[姓名]，专注于大模型后训练与 RAG 工程落地。

我的项目是医学 LLM 后训练 + Safety-RAG 系统。后训练部分：用 Qwen3-8B 走完了 SFT(58K QLoRA) → DPO(10K) 的完整训练链路，构建了 LLM-as-Judge 三维度评测体系。RAG 部分：构建了 50 万条医疗知识库(bge-large-zh+FAISS)，设计了 Safety-RAG 验证-修正闭环——检索→生成→verifier 事实比对→修正，相比标准 RAG 多了安全验证层。部署部分：LoRA merge 后用 vLLM 部署 API 服务，RTX 5090 上压测 877 tok/s、TTFT 42ms、32 并发。

我在项目中同时积累了后训练算法能力、RAG 工程化经验、模型部署实践——这三个方向是我求职的核心竞争力。
```

**记忆要点**：后训练 + RAG + 部署 三根支柱，每根有一个关键数字/技术点。

---

## 十七、医学项目工程落地 — 1分钟讲法

```
医学 LLM 项目我不仅做了后训练，还做了工程落地。训练完成后，我把 QLoRA adapter merge 到基座模型，用 vLLM 部署成 OpenAI-compatible API。在 RTX 5090 上做了性能压测：吞吐 877 tok/s，首 token 延迟 42ms，支持 32 路并发。我还对 RAG pipeline 做了延迟拆解——embedding 检索 ~8ms、向量检索 ~30ms、generation 占大头 ~60%，找到了瓶颈在 verifier（额外增加 ~300ms），设计了动态路由方案：简单问题走 light-RAG 省去 verifier，高风险问题走 full Safety-RAG。虽然项目没有真正上线，但 API 是生产就绪的，压测数据证明了可行性。
```

**记忆要点**：merge + 部署 + 压测数据（877/42/32） + 延迟拆解 + 优化方案（动态路由）。

---

## 十八、医学项目工程落地 — 3分钟讲法

```
【训练与 Merge】
训练完成后，我做的第一件事是把 QLoRA adapter 合并到基座模型。QLoRA 训练时只保存了 LoRA 权重（A 和 B 矩阵），推理时需要加载基座 + adapter 做 forward。Merge 的公式是 W' = W + (α/r) × BA，merge 后就是一个独立的完整模型，可以直接用 vLLM 加载，不需要额外推理开销。Merge 脚本用 PEFT 的 merge_and_unload() 方法，合并后模型大小从 4bit 恢复到 bf16（约 16GB）。

【vLLM 部署与压测】
部署方面我选择了 vLLM 而非 HuggingFace TGI——vLLM 的 PagedAttention 和 Continuous Batching 在并发场景下吞吐优势明显。启动参数：max-model-len=4096, gpu-memory-utilization=0.9, dtype=bfloat16, max-num-seqs=32。硬件是 RTX 5090 32GB——8B 模型 bf16 占约 16GB，KV cache 按 4096 context + 32 并发估算约 10-12GB，总计 26-28GB，刚好在 32GB 显存内。

压测我用 Python asyncio + OpenAI client 模拟并发请求，从 1 并发逐步增加到 64 并发。关键发现：32 并发前 TTFT 稳定在 40-50ms，吞吐线性增长到 877 tok/s；超过 32 并发后 TTFT 开始显著上升（排队效应），这是系统的饱和点。单用户场景 TTFT 42ms、TPOT ~15ms，用户体验流畅。

【RAG 延迟拆解与优化】
我对端到端 RAG pipeline 做了延迟拆解：query embedding（bge-large-zh on CPU）~8ms → FAISS 向量检索（50万条，FlatIP）~30ms → 检索结果拼接 prompt ~1ms → vLLM prefill（短 prompt ~200 tokens）~50ms → vLLM decode（avg 100 tokens output）~1.5s → Safety-RAG verifier（微调 Qwen3-8B 做 NLI 验证）~300ms。总延迟约 1.9s。

显然 generation 是最大头（~80%），verifier 次之（~15%）。优化思路：1）generation 用 streaming，TTFT 后用户立即看到 token，感知延迟大幅降低；2）verifier 做动态路由——简单/低风险问题走 light-RAG 省去 verifier，仅高风险问题触发验证，预计平均延迟降低 20-40%；3）多路召回（dense+sparse）用 asyncio.gather 并行化，延迟取 max 而非 sum。

【缓存和容灾设计（设计方案，未实施）】
虽然项目在本地环境，但我对生产级 RAG 的工程设计有完整方案：四层缓存体系（query cache / embedding cache / retrieval cache / rerank cache），用 Redis 做缓存存储，TTL 按知识库更新频率设定；动态路由规则引擎区分急症/用药/常识三类场景；Nginx 反向代理 + 多 vLLM 副本做负载均衡；Prometheus + Grafana 监控 p50/p95/p99 延迟和 GPU 利用率；熔断降级策略（vLLM 不可用→回退到轻量 CPU 模型）。

【总结】
虽然没有真实的线上环境，但我完成了从训练到部署到压测到优化的完整工程链路，并且对生产级系统需要什么（监控/灰度/容灾/缓存/路由）有清晰的理解和方案设计。这些工程实践和经验证明我具备将算法模型落地为可用服务的能力。
```

**记忆要点**：分四个子主题（训练merge→部署压测→延迟优化→缓存容灾），每个有具体数字和技术细节。

---

## 十九、"你有部署经验吗"回答模板

> 适用场景：面试官直接问部署经验

```
有的。我在医学 LLM 项目中完成了从训练到部署的完整链路：

第一，模型准备。QLoRA 训练后，我把 LoRA adapter merge 到基座模型（W' = W + (α/r)×BA），得到一个独立的 bf16 模型。这样 vLLM 可以直接加载，不需要 managed LoRA serving 的额外开销。

第二，vLLM 部署。我使用 vLLM 部署 API 服务，关键配置：max-model-len=4096, gpu-memory-utilization=0.9, max-num-seqs=32。API 兼容 OpenAI 格式（/v1/chat/completions），支持 streaming 和非 streaming。

第三，性能压测。我在 RTX 5090 (32GB) 上做了系统压测：吞吐 877 tok/s，TTFT 42ms，TPOT ~15ms，32 并发下稳定运行。找到了系统饱和点（32 并发），验证了显存预算（模型 16GB + KV cache ~12GB = 28GB < 32GB）。

第四，工程优化方案。虽然项目没有线上系统，但我设计了：RAG 延迟拆解和优化方案（streaming + 动态路由预期降低 20-40% 平均延迟）、多路召回 asyncio 并行化、四层缓存体系、Nginx 负载均衡架构。这些方案有设计文档但没有在真实生产环境中实施。

诚实地说，我的部署经验是"本地生产就绪"级别——API 可用、性能可测量、方案可扩展，但缺少真实用户流量、灰度发布、线上监控的实战经验。如果加入贵团队，我有扎实的基础可以快速上手生产级部署。
```

**为什么这样答**：
- 四个阶段展示了完整的部署思维（不只是"我跑了个 vLLM"）
- 有具体数字（877/42/32）证明不是编造
- 诚实区分"已做"和"已设计但未实施"
- 把"缺少经验"转化为"基础扎实+快速上手"

---

## 二十、"你有并行训练经验吗"回答模板

> 适用场景：面试官问是否有多卡/分布式训练经验
> 关键原则：诚实承认只有单卡 QLoRA 实践，但展示对分布式训练原理的理解

```
我的训练实践是基于单卡/双卡 QLoRA 完成的——医学 LLM 项目的 SFT 和 DPO 都在 2×4090 上使用 QLoRA 训练，不需要多机多卡的分布式训练。但我对分布式训练的原理和工具链有系统性的学习。

DDP (DistributedDataParallel)：我理解它的核心是每个 GPU 有完整模型副本，处理不同 micro-batch，backward 时自动触发 all-reduce 同步梯度。适用于模型能放进单卡显存的场景（如 8B 全量微调需要 4-8 卡 DDP）。

FSDP (Fully Sharded Data Parallel)：参数分片到各 GPU，前向/反向时按需 gather，省大量显存。和 DeepSpeed ZeRO-3 原理类似，但 FSDP 是 PyTorch 原生方案，配置更简单。

DeepSpeed ZeRO 三阶段：ZeRO-1 分片优化器状态，ZeRO-2 加上梯度分片，ZeRO-3 加上参数分片。ZeRO-3 理论上显存降到 1/N。我了解 ds_config.json 的关键配置：zero_optimization.stage、offload_optimizer、gradient_accumulation_steps 等。

torchrun 启动：--nproc_per_node 指定每节点 GPU 数，--nnodes 指定节点数，脚本内用 LOCAL_RANK 环境变量绑定 GPU。

我的现状是：小型实验用 QLoRA 单/双卡完全够用（QLoRA 只占 ~13GB 显存），所以没有机会实践真正的分布式训练。但如果需要做全量微调或更大规模模型（如 70B），我理解需要 ZeRO-3/FSDP + 多机多卡，并且我有足够的原理基础可以快速上手。如有可能，我可以在实习中补齐分布式训练的实战经验。
```

**为什么这样答**：
- 诚实第一：不说"我做过 DDP 训练"如果没做过
- "理论了解+实践空缺"的组合比"假装做过"更可信
- 展示了足够的原理理解，证明不是零基础
- 给出为什么没做的合理解释（QLoRA 不需要分布式）
- 表达了学习意愿

---

## 二十一、"你有线上系统经验吗"保守回答模板

> 适用场景：面试官追问线上部署经验
> 关键原则：诚实回答本地部署+压测的现状，展示对线上系统原理的理解

```
诚实地说，我的项目目前是"本地生产就绪"状态而非"线上运行"状态。具体来说：

已经完成的：1）QLoRA 训练 → LoRA merge → vLLM 部署 → API 服务化（OpenAI 兼容格式）；2）RTX 5090 上的性能压测和并发测试（吞吐 877 tok/s, TTFT 42ms, 32 并发饱和点）；3）延迟拆解和优化方案设计（streaming/动态路由/缓存/并行化）。

未完成的：真实的线上多节点部署、K8s 容器化、灰度发布（Canary/Blue-Green）、Prometheus+Grafana 线上监控告警、多副本负载均衡（Nginx）、熔断降级。这些是生产级系统的必备组件。

但我对这些生产级技术有系统性的理解：

线上监控：我知道核心监控四信号（Latency/Traffic/Errors/Saturation），Prometheus 通过 /metrics endpoint scrape 时序数据，Grafana 做可视化，p50/p95/p99 分位数延迟是比平均值更有意义的指标。

灰度发布：Canary 发布先切 5% 流量到新模型观察指标（延迟/安全违规率/用户满意度），稳定后逐步增加到 100%。如果出现问题，立即回滚到旧版本。Blue-Green 部署是两套完整环境切换，适合重大版本升级。

熔断降级：熔断器三个状态（Closed→Open→Half-Open），模型服务异常时触发熔断，回退到缓存回答或静态 FAQ。降级分级：Level 1（vLLM→轻量CPU模型）→ Level 2（→静态FAQ检索）→ Level 3（→预设降级消息）。

负载均衡：Nginx upstream 多 vLLM 实例 + health check + 最少连接策略。一致性哈希可用于同一用户的请求路由到同一副本。

总结：我有完整的"本地部署+压测+优化设计"经验，对"线上生产级"有清晰的认知和方案设计能力，缺的是真实线上环境的实战机会。如果团队有生产系统，我可以快速学习并贡献。
```

**为什么这样答**：
- 明确区分"已完成"和"未完成"，建立信任
- 对未完成的部分展示原理理解（不是完全不懂）
- 每个线上技术都给出了具体理解，证明不是"听说过名词"
- 把"没有线上经验"转化为"有原理基础，需要实战机会"

---

## 二十二、"项目还没上线怎么体现落地能力"回答模板

> 适用场景：面试官质疑"这只是个 demo/实验室项目，如何证明能落地？"

```
我理解您关心的是项目是否具备实际工程价值。虽然项目没有上线，但我在以下几个层面证明了落地能力：

第一，后训练闭环本身就是一种工程落地。不是说把模型部署出去才叫落地——从数据生成策略设计、训练 pipeline 搭建、超参数决策、问题发现与修复，这一整套"做出来+分析明白+修正好"的闭环就是算法工程的落地。我完成了 4 个阶段可独立运行的模块化 pipeline，这是工程思维的体现。

第二，LoRA merge + vLLM 部署证明了模型服务化的可行性。我合并不是简单地 run 一个脚本——我做了：merge 方案选择（merge vs 动态 serving 的 trade-off）、显存预算计算（16GB 模型 + 12GB KV cache = 28GB < 32GB）、压测方案设计（1→64 并发梯度测试）、饱和点分析（32 并发处 TTFT 开始上升）。这些就是推理部署工程的核心工作。

第三，Safety-RAG 本身就是工程化设计。不是简单的"检索了然后拼接"——而是有 verifier、有动态路由、有缓存设计、有延迟拆解、有优化方案的完整工程系统。这些设计即使没有部署到 K8s 集群，也已经体现了系统设计能力。

第四，可扩展方案设计。我虽然没有多节点集群，但对 Nginx 负载均衡、Prometheus 监控、灰度发布、熔断降级有清晰的方案设计。这些方案可以直接指导后续的上线工作——不是从零摸索。

总结来说：我的落地能力体现在"后训练闭环的工程完备性 + 部署压测的实操经验 + Safety-RAG 的系统设计 + 生产级方案的先验知识"。项目没上线不是因为能力不够，而是资源和场景限制。给我一个生产环境，我可以用这些积累快速完成从模型到服务的交付。
```

**为什么这样答**：
- 不回避"没上线"的事实，但重新定义"落地"的维度
- 四个维度（训练闭环/部署压测/系统设计/方案储备）展示了完整的工程视野
- "不是能力不够，而是资源限制"——把弱势转化为对机会的渴望

---

## 使用建议

1. **录制音频练习**：把自己的项目讲述录下来，回听，找到不流畅的地方反复练习
2. **控制时间**：用秒表计时，确保每个版本都在时间预算内
3. **数字要准确**：58K、10K、8B、0.1、66.7%、0.78、12GB——这些数字要能解释来源
4. **叙事要有起伏**：项目叙述的结构应该是"做了什么 → 发现了什么意外 → 怎么解决的 → 学到了什么"
5. **诚实是底线**：不要夸大样本量、不要说"显著性"如果没有统计检验、不要说"上线了"如果只是测试
6. **根据不同面试官调整**：算法面试官→多讲方法原理和设计决策；工程面试官→多讲工程优化和架构设计；综合面试官→讲完整故事和 impact
7. **工程版本特别建议**：诚实区分"已做""已设计未实施""了解原理"三个层次，使用具体的数字和指标（877 tok/s / 42ms / 32并发）建立可信度

---

## 背诵版总结

1. **30秒版**：姓名+学校+方向+两个项目一句话+求职意向
2. **1分钟版**：在30秒基础上加入医学项目的核心流程(teacher→SFT→DPO→RAG)
3. **2分钟版**：加入关键数据(58K/10K/8B/66.7%)和关键发现(DPO长尾幻觉→Safety-RAG)
4. **项目1分钟版**：做了什么+用了什么技术+发现了什么+怎么解决的
5. **项目3分钟版**：完整STAR叙述(背景→任务→行动→结果→反思)
6. **项目5分钟版**：加入技术细节(beta=0.1, rank=16, QLoRA, hybrid retrieval, 877 tok/s)
7. **MiniMind 30秒**：从零训练的小模型项目，证明理解底层原理，辅助角色
8. **工程落地版（新增）**："算法+工程"1分钟 / "后训练+RAG+部署"1分钟 / 工程落地1分钟&3分钟 / 部署经验回答 / 并行训练回答 / 线上系统回答 / 落地能力回答
9. **必背数字**：58K/10K/8B/0.1/16/66.7%/4.96/877/42/11707
10. **必背话术**："这不是简单的跑脚本——数据构造策略、DPO对构造逻辑、幻觉发现与修复闭环才是算法核心"
11. **工程诚实话术**："我的部署经验是本地生产就绪级别——API可用、性能可测量、方案可扩展，缺的是真实流量和线上环境的实战机会"
12. **练习方法**：对镜子讲、录音回放、找人模拟，直到脱稿流畅
